<!DOCTYPE html><html lang="ar"><head><meta charset="UTF-8" /><title>حاسبة أسعار الشحن</title><style>  body { font-family: Arial, sans-serif; direction: rtl; padding: 20px; max-width: 400px; margin: auto; }  label, input, button { display: block; width: 100%; margin-bottom: 15px; }  button { padding: 10px; background-color: #007bff; color: white; border: none; cursor: pointer; }  button:hover { background-color: #0056b3; }  #result { font-weight: bold; font-size: 1.2em; }</style></head><body><h2>حاسبة أسعار الشحن - Shein فقط</h2><label for="price">سعر المنتج بالدولار ($):</label><input type="number" id="price" min="0" step="0.01" placeholder="ادخل سعر المنتج" /><label for="weight">الوزن بالكيلو (Kg):</label><input type="number" id="weight" min="0" step="0.1" placeholder="ادخل الوزن" /><button onclick="calculatePrice()">احسب السعر النهائي</button><div id="result"></div><script>  function roundToNearest250(num) {    return Math.round(num / 250) * 250;  }  function calculatePrice() {    const exchangeRate = 1380; // سعر الصرف ثابت    const priceUSD = parseFloat(document.getElementById('price').value);    const weight = parseFloat(document.getElementById('weight').value);    if (isNaN(priceUSD) || priceUSD <= 0 || isNaN(weight) || weight <= 0) {      document.getElementById('result').textContent = 'يرجى إدخال قيم صحيحة للسعر والوزن.';      return;    }    // حساب تكلفة الشحن حسب الوزن    let shippingCost = 0;    if (weight < 10) {      shippingCost = weight * 3500;    } else {      shippingCost = 50000;    }    // حساب السعر الكلي بالدينار    const productCost = priceUSD * exchangeRate;    let totalPrice = productCost + shippingCost;    // تقريب السعر لأقرب 250 دينار    totalPrice = roundToNearest250(totalPrice);    document.getElementById('result').textContent =       `السعر النهائي المتوقع: ${totalPrice.toLocaleString()} دينار عراقي`;  }</script></body></html>